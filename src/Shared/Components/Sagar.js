import React from 'react'

export default function Sagar({ heading, body }) {    
    return (
        <div className="something">
            <h1>{heading}</h1>
            <p>{body}</p>
        </div>
    )
}
